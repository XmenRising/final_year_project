specVersion: "v1beta"
serviceId: "finalyearproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "finalyearproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
